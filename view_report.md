elf-tui 深度审查：逻辑闭环与漏洞研究能力评估

第一部分：现有架构的本质理解

1.1 工具定位：从底层原理看

elf-tui 本质上是一个 以 TUI 为交互界面的多模态 ELF 分析数据聚合器。其底层逻辑链路是：

mmap(ELF) ─→ Elf64_Ctx (字节级随机访问)
                │
     ┌──────────┼──────────┐
     ▼          ▼          ▼
  静态解析    Capstone    SQLite3
 (结构提取)  (语义提取)  (关系存储)
     │          │          │
     └──────────┼──────────┘
                ▼
          PanelData
          (格式化文本行)
                │
                ▼
         notcurses 渲染

核心洞察：整个系统的中间表示（IR）是 PanelData——一个格式化文本行的动态数组。这意味着所有分析结果在存储到 SQLite 之前，已经被"扁平化"为人类可读的字符串，丢失了结构化的语义信息。DB 层的 15 张表是独立维护的第二套 IR，但 DB 查询和分析模块之间的数据流是断裂的——DB 导入是一次性的，之后的分析模块（danger、taint、rop_chain）各自独立扫描 mmap 区域，不消费 DB 中已有的结构化数据。

这是一个关键的架构性问题：DB 是数据的坟墓，不是分析的中枢。数据被导入后，除了地址查询（db_query_addr_all），没有分析模块将 DB 作为推理引擎使用。

1.2 模块分层评价

┌──────────────┬────────┬─────────────────────────────────────────────────────────────┐
│      层      │ 完成度 │                            评价                             │
├──────────────┼────────┼─────────────────────────────────────────────────────────────┤
│ ELF 结构解析 │ 90%    │ 覆盖 ELF64 全部标准结构，类型常量齐全                       │
├──────────────┼────────┼─────────────────────────────────────────────────────────────┤
│ 反汇编+CFG   │ 70%    │ 有 Capstone 集成、基本块切分、CFG 边，但缺少中间语言提升    │
├──────────────┼────────┼─────────────────────────────────────────────────────────────┤
│ 符号系统     │ 60%    │ 有导入符号、QueryDB 查找，但无类型传播、无结构体重建        │
├──────────────┼────────┼─────────────────────────────────────────────────────────────┤
│ 动态调试     │ 50%    │ ptrace attach/step/continue 可用，但无内存监控、无 trace 录 │
├──────────────┼────────┼─────────────────────────────────────────────────────────────┤
│ 漏洞检测     │ 25%    │ 仅危险函数匹配 + 基础污点（函数内、不跨过程）               │
├──────────────┼────────┼─────────────────────────────────────────────────────────────┤
│ 利用开发     │ 15%    │ Gadget 搜索 + 简单 ROP 链编译，无约束求解、无堆利用规划     │
└──────────────┴────────┴─────────────────────────────────────────────────────────────┘

---
第二部分：漏洞研究生命周期的闭环缺口分析

一个完整的二进制漏洞研究工作流包含以下阶段。我对照 elf-tui 当前能力逐一评估：

Phase 1: 目标侦察与信息收集

┌─────────────────────────┬──────────────────────────────────┬───────────────────────────────────────────────┐
│       需要的能力        │           elf-tui 现状           │                     缺口                      │
├─────────────────────────┼──────────────────────────────────┼───────────────────────────────────────────────┤
│ ELF 结构全景            │ ✅ ehdr/phdr/shdr 完整           │ —                                             │
├─────────────────────────┼──────────────────────────────────┼───────────────────────────────────────────────┤
│ 加固措施检测            │ ✅ security.c — RELRO/NX/PIE/CET │ 缺少：CFI (clang) 检测、ShadowStack 检测      │
├─────────────────────────┼──────────────────────────────────┼───────────────────────────────────────────────┤
│ 编译器指纹              │ ❌                               │ 无法识别 GCC/Clang 版本、优化级别             │
├─────────────────────────┼──────────────────────────────────┼───────────────────────────────────────────────┤
│ 第三方库识别与 CVE 匹配 │ ❌                               │ 无 .gnu.version_r 深度解析、无 CVE 数据库关联 │
├─────────────────────────┼──────────────────────────────────┼───────────────────────────────────────────────┤
│ 二进制差异比对          │ ❌                               │ 无 patch diff、无版本间函数匹配               │
└─────────────────────────┴──────────────────────────────────┴───────────────────────────────────────────────┘

Phase 2: 攻击面映射

┌─────────────────────┬─────────────────────────────────────────────────┬─────────────────────────────────────────────┐
│     需要的能力      │                  elf-tui 现状                   │                    缺口                     │
├─────────────────────┼─────────────────────────────────────────────────┼─────────────────────────────────────────────┤
│ 导入函数分类        │ ✅ attack_surface.c — 7 大类                    │ 粒度粗，缺少参数级别的攻击面评估            │
├─────────────────────┼─────────────────────────────────────────────────┼─────────────────────────────────────────────┤
│ 系统调用面          │ ⚠️ 静态 syscall 表有，但无实际 syscall 指令枚举 │ 缺少：从反汇编中提取所有 syscall 并分析参数 │
├─────────────────────┼─────────────────────────────────────────────────┼─────────────────────────────────────────────┤
│ 网络协议解析面      │ ❌                                              │ 无协议格式推断、无状态机恢复                │
├─────────────────────┼─────────────────────────────────────────────────┼─────────────────────────────────────────────┤
│ 文件格式解析面      │ ❌                                              │ 无 parser 识别、无格式感知 fuzz             │
├─────────────────────┼─────────────────────────────────────────────────┼─────────────────────────────────────────────┤
│ IPC/D-Bus/Binder 面 │ ❌                                              │ 仅限传统 Unix IPC                           │
├─────────────────────┼─────────────────────────────────────────────────┼─────────────────────────────────────────────┤
│ 权限边界分析        │ ❌                                              │ 无 setuid/capability 相关代码路径追踪       │
├─────────────────────┼─────────────────────────────────────────────────┼─────────────────────────────────────────────┤
│ IOCTL 接口枚举      │ ❌                                              │ 无 ioctl cmd 值提取和分类                   │
└─────────────────────┴─────────────────────────────────────────────────┴─────────────────────────────────────────────┘

Phase 3: 静态深度分析（这里是最大短板）

┌────────────────────────┬──────────────────────────────────────┬──────────────────────────────────────────────┐
│       需要的能力       │             elf-tui 现状             │                     缺口                     │
├────────────────────────┼──────────────────────────────────────┼──────────────────────────────────────────────┤
│ 反汇编                 │ ✅ Capstone 集成                     │ 无指令语义提升为 IR                          │
├────────────────────────┼──────────────────────────────────────┼──────────────────────────────────────────────┤
│ 函数边界识别           │ ✅ func_boundary.c                   │ 但函数类型签名未恢复                         │
├────────────────────────┼──────────────────────────────────────┼──────────────────────────────────────────────┤
│ 调用图                 │ ✅ callgraph.c                       │ 仅 caller→callees，无间接调用解析            │
├────────────────────────┼──────────────────────────────────────┼──────────────────────────────────────────────┤
│ 控制流图               │ ✅ cfg_view.c + DB 中的 bb/cfg_edges │ 仅可视化，无路径分析                         │
├────────────────────────┼──────────────────────────────────────┼──────────────────────────────────────────────┤
│ 数据流分析 (def-use)   │ ❌                                   │ 这是最关键的缺失。无到达定义、无活跃变量分析 │
├────────────────────────┼──────────────────────────────────────┼──────────────────────────────────────────────┤
│ 值域分析 (value range) │ ❌                                   │ 无法判断循环边界、缓冲区大小约束             │
├────────────────────────┼──────────────────────────────────────┼──────────────────────────────────────────────┤
│ 类型传播/恢复          │ ❌                                   │ 无法从上下文推断结构体字段类型               │
├────────────────────────┼──────────────────────────────────────┼──────────────────────────────────────────────┤
│ 间接调用目标解析       │ ❌                                   │ Vtable/函数指针目标未分析                    │
├────────────────────────┼──────────────────────────────────────┼──────────────────────────────────────────────┤
│ 别名分析               │ ❌                                   │ 指针别名关系不明，导致污点追踪保守           │
├────────────────────────┼──────────────────────────────────────┼──────────────────────────────────────────────┤
│ 全局变量交叉引用       │ ⚠️ xref.c 有基础 xref                │ 但无读/写分类、无数据流                      │
└────────────────────────┴──────────────────────────────────────┴──────────────────────────────────────────────┘

原理性说明：静态漏洞发现的本质是 不变量检测——证明程序在某个点上违反了安全约束。这需要：
1. 值域分析：变量 X 的取值范围是什么？
2. 约束传播：条件分支如何约束后续代码？
3. 边界检查建模：缓冲区大小 N 与索引变量 I 的关系是什么？

当前 elf-tui 的静态分析停留在语法层面（哪些指令、哪些函数调用），完全没有进入语义层面（这些指令在计算什么、值的范围是什么）。danger.c 只能告诉你"这里调用了 strcpy"，但无法告诉你"源缓冲区和目标缓冲区的大小关系是否安全"。

Phase 4: 动态分析

┌─────────────────────────┬────────────────────────────┬─────────────────────────────────────────────────┐
│       需要的能力        │        elf-tui 现状        │                      缺口                       │
├─────────────────────────┼────────────────────────────┼─────────────────────────────────────────────────┤
│ 进程附加                │ ✅ ptrace ATTACH           │ —                                               │
├─────────────────────────┼────────────────────────────┼─────────────────────────────────────────────────┤
│ 寄存器查看              │ ✅ 实时刷新                │ —                                               │
├─────────────────────────┼────────────────────────────┼─────────────────────────────────────────────────┤
│ 单步/继续               │ ✅ step/continue/step-over │ —                                               │
├─────────────────────────┼────────────────────────────┼─────────────────────────────────────────────────┤
│ 软件断点                │ ✅ INT3                    │ —                                               │
├─────────────────────────┼────────────────────────────┼─────────────────────────────────────────────────┤
│ 硬件断点                │ ✅ DR0-DR7                 │ —                                               │
├─────────────────────────┼────────────────────────────┼─────────────────────────────────────────────────┤
│ 内存读写                │ ✅ debug_readmem/writemem  │ —                                               │
├─────────────────────────┼────────────────────────────┼─────────────────────────────────────────────────┤
│ 指令级执行 trace        │ ❌                         │ 这是巨大的缺失。无 single-step trace 录制       │
├─────────────────────────┼────────────────────────────┼─────────────────────────────────────────────────┤
│ 内存访问监控            │ ❌                         │ 无 watchpoint（非仅断点，而是记录所有内存访问） │
├─────────────────────────┼────────────────────────────┼─────────────────────────────────────────────────┤
│ 覆盖率追踪              │ ❌                         │ 无 basic block 覆盖率、无路径覆盖率             │
├─────────────────────────┼────────────────────────────┼─────────────────────────────────────────────────┤
│ 调用追踪                │ ⚠️ 有 backtrace（RBP 链）  │ 但无完整 call/ret trace                         │
├─────────────────────────┼────────────────────────────┼─────────────────────────────────────────────────┤
│ 堆分配追踪              │ ⚠️ 有 snapshot             │ 但无 hook malloc/free 实现全量分配日志          │
├─────────────────────────┼────────────────────────────┼─────────────────────────────────────────────────┤
│ 信号处理追踪            │ ❌                         │ 无 signal handler 调用链                        │
├─────────────────────────┼────────────────────────────┼─────────────────────────────────────────────────┤
│ 多线程                  │ ❌                         │ 无 thread-aware 调试                            │
├─────────────────────────┼────────────────────────────┼─────────────────────────────────────────────────┤
│ 时间旅行调试            │ ❌                         │ 无 record/replay                                │
├─────────────────────────┼────────────────────────────┼─────────────────────────────────────────────────┤
│ 比较执行 (differential) │ ❌                         │ 无法对比两次运行的差异                          │
└─────────────────────────┴────────────────────────────┴─────────────────────────────────────────────────┘

原理性说明：现代漏洞研究中的动态分析核心是 trace-based analysis——录制完整的执行轨迹后离线分析。当前 elf-tui 的调试模式是交互式的（人工看着寄存器单步），缺乏自动化 trace 采集和分析能力。漏洞触发往往是瞬间的（一个特定的输入在特定的代码路径上触发），交互式调试无法捕获。

Phase 5: 漏洞发现（静态 + 动态结合）

┌──────────────┬─────────────────────────────────┬──────────────────────────────────────────────┐
│   漏洞类型   │        elf-tui 检测能力         │              实际需要的检测方法              │
├──────────────┼─────────────────────────────────┼──────────────────────────────────────────────┤
│ 栈缓冲区溢出 │ ⚠️ 危险函数标记                 │ 需要：目标缓冲区大小 vs 源数据大小的约束求解 │
├──────────────┼─────────────────────────────────┼──────────────────────────────────────────────┤
│ 堆缓冲区溢出 │ ⚠️ 危险函数标记                 │ 需要：堆分配大小追踪 + 访问范围检查          │
├──────────────┼─────────────────────────────────┼──────────────────────────────────────────────┤
│ 整数溢出     │ ⚠️ calloc/malloc 标记           │ 需要：值域分析 + 溢出后的截断检查            │
├──────────────┼─────────────────────────────────┼──────────────────────────────────────────────┤
│ 格式化字符串 │ ⚠️ 危险函数标记                 │ 需要：数据流分析——fmt 参数是否来自用户输入   │
├──────────────┼─────────────────────────────────┼──────────────────────────────────────────────┤
│ UAF          │ ⚠️ heap_analyzer 一致性检查     │ 需要：完整的 alloc/free trace + 悬挂指针追踪 │
├──────────────┼─────────────────────────────────┼──────────────────────────────────────────────┤
│ Double Free  │ ⚠️ heap_analyzer free list 检查 │ 较完善，但依赖 snapshot 而非 trace           │
├──────────────┼─────────────────────────────────┼──────────────────────────────────────────────┤
│ 竞态条件     │ ⚠️ TOCTOU 函数对标记            │ 需要：并发路径分析或动态 race detector       │
├──────────────┼─────────────────────────────────┼──────────────────────────────────────────────┤
│ 类型混淆     │ ❌                              │ 需要：类型传播 + 类型转换点检测              │
├──────────────┼─────────────────────────────────┼──────────────────────────────────────────────┤
│ 未初始化内存 │ ❌                              │ 需要：定义-使用分析 + 动态 valgrind 集成     │
├──────────────┼─────────────────────────────────┼──────────────────────────────────────────────┤
│ 信息泄漏     │ ⚠️ 输出函数标记                 │ 需要：数据流——未初始化/越界数据是否流向输出  │
├──────────────┼─────────────────────────────────┼──────────────────────────────────────────────┤
│ 逻辑漏洞     │ ❌                              │ 需要：语义理解，难以自动化                   │
└──────────────┴─────────────────────────────────┴──────────────────────────────────────────────┘

Phase 6: 根因分析 (Root Cause Analysis)

当前 elf-tui 对 crash 的处理：fuzz_analyze_crash() 做了基本的信号分类和寄存器值检查，但缺少：

- Crash 点的数据流回溯：这个 crash 地址的值是从哪里来的？
- 输入到 crash 的约束推导：什么样的输入字节触发了这个 crash？
- Crash 输入的自动最小化：delta-debugging 或类似算法
- Crash 聚类/去重：基于调用栈的 crash 分组（当前仅基于 signal+addr）

Phase 7: 可利用性评估

┌──────────────────────────────────────────────────┬──────────────────────────────────────────────┐
│                    需要的分析                    │                 elf-tui 现状                 │
├──────────────────────────────────────────────────┼──────────────────────────────────────────────┤
│ 漏洞原语分类 (Arbitrary Read/Write/Control Flow) │ ❌                                           │
├──────────────────────────────────────────────────┼──────────────────────────────────────────────┤
│ 缓解措施对当前漏洞的有效性评估                   │ ⚠️ security.c 有独立检测，但未关联到具体漏洞 │
├──────────────────────────────────────────────────┼──────────────────────────────────────────────┤
│ 堆布局可控性分析                                 │ ⚠️ heap_analyzer 有 snapshot                 │
├──────────────────────────────────────────────────┼──────────────────────────────────────────────┤
│ 栈 pivoting 候选点                               │ ❌                                           │
├──────────────────────────────────────────────────┼──────────────────────────────────────────────┤
│ 信息泄漏路径分析                                 │ ❌                                           │
└──────────────────────────────────────────────────┴──────────────────────────────────────────────┘

Phase 8: Exploit 开发

┌─────────────────────────┬─────────────────┬───────────────────────────────────────┐
│       需要的工具        │  elf-tui 现状   │                 评价                  │
├─────────────────────────┼─────────────────┼───────────────────────────────────────┤
│ ROP gadget 搜索         │ ✅ gadget.c     │ 只有 pop/mov/syscall 三类，分类不完整 │
├─────────────────────────┼─────────────────┼───────────────────────────────────────┤
│ ROP 链编译              │ ⚠️ rop_chain.c  │ 仅支持按序拼接，无约束求解            │
├─────────────────────────┼─────────────────┼───────────────────────────────────────┤
│ One-gadget 搜索         │ ✅ one_gadget.c │ —                                     │
├─────────────────────────┼─────────────────┼───────────────────────────────────────┤
│ GOT 覆写规划            │ ❌              │ 无                                    │
├─────────────────────────┼─────────────────┼───────────────────────────────────────┤
│ 堆排布 (heap feng shui) │ ❌              │ 无                                    │
├─────────────────────────┼─────────────────┼───────────────────────────────────────┤
│ 格式化字符串利用生成    │ ❌              │ 无                                    │
├─────────────────────────┼─────────────────┼───────────────────────────────────────┤
│ Shellcode 嵌入          │ ❌              │ 无                                    │
├─────────────────────────┼─────────────────┼───────────────────────────────────────┤
│ ret2dl / ret2csu / SROP │ ❌              │ 无                                    │
├─────────────────────────┼─────────────────┼───────────────────────────────────────┤
│ ASLR 绕过规划           │ ❌              │ 无                                    │
└─────────────────────────┴─────────────────┴───────────────────────────────────────┘

---
第三部分：逻辑闭环的关键断点

从漏洞研究全流程来看，elf-tui 存在以下 关键数据流断点：

断点 1：静态分析 → 漏洞假设，之间没有数据流引擎

当前:  danger.c → 危险函数列表（仅API名称匹配）
       taint.c  → source→sink 路径（仅函数内、仅寄存器传播）

缺失:  值域分析 → 约束求解 → 边界检查验证
       "这个 strcpy 调用的目标缓冲区是 32 字节，源数据来自 recv()
        的第 2 个参数，recv 的 size 参数来自用户输入包的第 4 字节
        → 如果用户设置 size > 32，则栈溢出"

这不是简单的模式匹配能解决的，需要真正的 过程间数据流分析 + 值域传播。

断点 2：动态执行 → 漏洞确认，之间没有 trace 分析

当前:  ptrace 交互式调试 → 人工观察寄存器
       fuzz → crash → 简单信号分类

缺失:  执行 trace 录制 → 离线回放 → 自动异常检测
       "执行了 12345 条指令后，在 0x401234 处 rdi=0x41414141，
        该值来自 0x401100 处从 [rsp+8] 加载，而 [rsp+8] 被
        0x400f00 处的 recv() 写入 → 用户可控的 RIP"

断点 3：漏洞发现 → 利用开发，之间没有可利用性推理

当前:  发现危险函数 / crash → [人工分析] → 手工构造 exploit

缺失:  漏洞原语自动提取 → 缓解措施自动评估 → 利用策略推荐
       "UAF @ 0x555555558000, chunk size=0x40 (tcache bin 2),
        tcache->next 可控, 无 ASLR (PIE disabled), Full RELRO,
        → 建议: tcache poisoning → __free_hook → system()
        需要: heap leak 或 bruteforce 4-bit ASLR"

断点 4：所有分析结果 → 知识沉淀，之间没有知识提取

当前:  分析结果以 PanelData 文本展示 → [人工阅读] → 人工更新 memory

缺失:  结构化漏洞报告自动生成
       → 漏洞模式自动提取到 knowledge/
       → 后续相同模式自动匹配

---
第四部分：垂直深化路线图

以下按 投入产出比 和 对研究能力的提升幅度 排序：

P0（地基性工作——决定工具能走多远）

1. 统一中间表示 (IR) — 最根本的架构升级

现状：每个分析模块独立解析 mmap 区域，产物散落在 PanelData 文本和 SQLite 中。

深化方向：建立一个进程内的结构化 IR，所有分析模块消费和产出这个 IR：

                     ┌─→ 值域分析 ─→ 边界检查验证
IR (指令序列+CFG) ───┼─→ 数据流分析 ─→ def-use 链
                     ├─→ 类型传播 ──→ 间接调用解析
                     ├─→ 污点追踪 ──→ source→sink 路径
                     └─→ 利用分析 ──→ gadget 分类 + 链求解

技术选型建议：
- 不要从零构建 IR。集成一个已有的中间语言，如 VEX IR（来自 Valgrind，Apache 2.0 许可）或 BAP Core（来自 CMU，MIT 许可）
- VEX IR 的优势：成熟稳定，处理了所有 x86-64 指令的提升，直接可做数据流分析
- 将 VEX IR 的语句存入 SQLite 新表 ir_statements，用 SQL 做初步的 use-def 查询

为什么这是 P0：没有 IR，所有"深化"都是空谈。你不可能在汇编文本字符串上做数据流分析。

2. 过程间数据流分析引擎

现状：taint.c 仅在单个函数内追踪寄存器传播（保守策略：call 是边界）。

深化方向：
- Def-Use 链构建：对于每个内存写入，找到所有可能读取该位置的指令
- 过程间传播：跨函数边界追踪数据流，使用调用图作为骨架
- 值域分析：基于抽象解释（abstract interpretation）推断每个变量在每点的可能取值范围
- 堆抽象：建模 malloc/free 分配，追踪堆指针

具体产出：
- src → sink 的完整路径（跨 N 个函数调用）
- 缓冲区大小 vs 访问范围的约束求解
- 间接调用目标的多层解析

P1（研究效率倍增器）

3. 执行 Trace 录制与离线分析

原理：ptrace single-step 太慢（1000x 开销），但 Intel PT (Processor Trace) 可以硬件级记录控制流，< 1% 开销。

深化方向：
- 集成 Intel PT（通过 Linux perf_event_open）录制执行 trace
- 离线解码 PT trace → 基本块序列
- Trace 分析：覆盖率统计、路径探索、异常检测
- 与静态 CFG 对比：哪些路径被实际执行了？哪些从未到达？

实战价值：Fuzz 时同时录制 PT trace，crash 后回放完整的执行路径，一键定位根因。

4. 堆分析深化——从 snapshot 到 trace-based 分析

现状：heap_analyzer.c + heap_ptmalloc.c 做一次性的堆快照分析。

深化方向：
- LD_PRELOAD hook 或 ptrace syscall 拦截：记录每次 malloc/free/realloc 的完整参数和返回值
- 堆操作回放：将 alloc/free trace 可视化为时间轴
- 堆排布模拟：给定 alloc/free 序列，预测堆布局
- 利用约束求解：给定目标（覆写 __free_hook / 劫持 tcache），自动规划 alloc/free 序列
- 多分配器支持：完成 jemalloc/tcmalloc 解析器

5. Fuzz 引擎深化——从盲测到灰盒

现状：fuzz_engine.c 是基础的 mutation-based fuzzer，fork+ptrace 执行。

深化方向：
- 覆盖率引导：集成 PT trace 或插桩，记录每个输入的 BB 覆盖率
- 种子调度：基于覆盖率的种子评分和调度（AFL 风格的 bitmap）
- Harness 自动生成：从函数签名自动生成 fuzz harness
- 多线程 Fuzz：fork-server 模型并行 fuzz
- Sanitizer 协作：与 ASAN/UBSAN 编译的目标协作（通过 LD_PRELOAD 或远程）

P2（能力边界扩展）

6. 二进制差异比对

深化方向：
- Patch diff：对比补丁前后的二进制，定位修复的漏洞（1-day 研究核心能力）
- 版本 diff：跨版本函数匹配（基于 CFG 相似度、调用图结构）
- 供应链分析：识别二进制中的已知库版本 + 已公开漏洞匹配

技术路径：
- 函数级相似度：CFG 拓扑匹配 + 指令序列 n-gram 相似度
- 与 CVE 数据库关联：从 .gnu.version_r 提取库版本 → 查询 CVE

7. 符号执行集成

深化方向：
- 集成 angr（Python）或 SymCC（C++）做符号执行
- 对特定函数做路径探索，自动生成到达特定代码的输入
- 约束求解辅助 ROP 链生成（需要哪些 gadget 才能设置所有寄存器？）

注意：符号执行是"重量级"工具，不建议从零构建，应该集成现有成熟引擎。

8. 漏洞知识库自动化

深化方向：
- 漏洞检测结果 → 自动匹配 knowledge/vuln-patterns/ 中的模式
- 新模式发现时 → 自动提示更新 knowledge/
- 漏洞报告自动生成（结构化 JSON + Markdown）

---
第五部分：现有模块的垂直深化建议

针对几个核心模块的"从能用变成好用"的具体方向：

danger.c → 语义漏洞检测引擎

- 现状：API 名称匹配 + 调用点枚举
- 深化第 1 步：对每个危险调用点，反向追踪参数来源（数据流）
- 深化第 2 步：对缓冲区操作，提取目标缓冲区大小和源大小，做约束比较
- 深化第 3 步：输出置信度评分（"strcpy 但 src 是常量字符串 → 低风险" vs "strcpy 但 src 来自 recv() 且目标缓冲区 32 字节 → 高风险"）

taint.c → 过程间多源污点引擎

- 现状：函数内、仅寄存器、call 是边界
- 深化第 1 步：跨函数——call 指令不截断，追踪到子函数参数和返回值
- 深化第 2 步：内存污点——通过指针写入的内存也被标记，通过指针读取时检测
- 深化第 3 步：污点策略——用户可自定义 source/sink 规则（配置文件或 DSL）

heap_analyzer.c → 堆利用工作站

- 现状：snapshot + 一致性检查 + 基本可视化
- 深化第 1 步：alloc/free trace（见 P1）
- 深化第 2 步：交互式堆操作模拟——"如果我分配 3 个 0x40 的 chunk 再 free 第 2 个，tcache 布局如何变化？"
- 深化第 3 步：自动利用建议——"这个 double free 可以做 tcache poisoning → 覆写 __free_hook"

rop_chain.c → 约束求解型 ROP 编译器

- 现状：按序拼接 gadget，不支持复杂约束
- 深化第 1 步：约束建模——"我需要 rdi=X, rsi=Y, rdx=Z，rax=59，且 rsp 16 字节对齐"
- 深化第 2 步：自动搜索满足约束的最小 gadget 组合
- 深化第 3 步：多策略并行——system()、execve syscall、one_gadget、SROP——自动选择最优

gadget.c → 语义 Gadget 数据库

- 现状：只分类 pop/mov/syscall，类型粗糙
- 深化第 1 步：完整语义分类——load/store/arith/logic/branch/syscall/stack-pivot
- 深化第 2 步：Gadget 质量评分——副作用（clobber 哪些寄存器？）、栈消耗量
- 深化第 3 步：非 ret 结尾的 gadget（jmp [reg]、call [reg]）纳入搜索

db.c → 分析中枢

- 现状：数据存储，一次性导入，查询有限
- 深化第 1 步：图查询——"从 recv() 调用点到 strcpy() 调用点之间，CFG 上的所有路径"
- 深化第 2 步：增量更新——不是全量重新导入，而是追加新数据
- 深化第 3 步：跨实体关联查询——"所有修改全局变量 g_buf 的指令"、"所有读取 g_buf 的指令"

---
第六部分：优先级建议

从 研究产出效率 角度，我建议的优先级：

┌────────┬─────────────────────────────┬──────────────────────────────────────────────────────────────┐
│ 优先级 │            项目             │                             理由                             │
├────────┼─────────────────────────────┼──────────────────────────────────────────────────────────────┤
│ P0-1   │ 统一 IR 引入 (VEX IR)       │ 所有深度分析的基础设施，没有它后续都是"语法糖"               │
├────────┼─────────────────────────────┼──────────────────────────────────────────────────────────────┤
│ P0-2   │ 执行 Trace 录制 (Intel PT)  │ 将动态分析从"人工单步"变为"自动录制+离线分析"，效率提升 100x │
├────────┼─────────────────────────────┼──────────────────────────────────────────────────────────────┤
│ P1-1   │ 跨过程数据流分析            │ 将漏洞检测从"API 名称匹配"升级为"语义级漏洞发现"             │
├────────┼─────────────────────────────┼──────────────────────────────────────────────────────────────┤
│ P1-2   │ Fuzz 覆盖率引导             │ 灰盒 fuzz 的产出远超盲 fuzz，且实现成本可控                  │
├────────┼─────────────────────────────┼──────────────────────────────────────────────────────────────┤
│ P1-3   │ 堆分配 Trace + 分析         │ 堆利用研究目前完全依赖人工，自动化后效率质变                 │
├────────┼─────────────────────────────┼──────────────────────────────────────────────────────────────┤
│ P2-1   │ 二进制差异比对 (patch diff) │ 1-day 漏洞研究的核心工具                                     │
├────────┼─────────────────────────────┼──────────────────────────────────────────────────────────────┤
│ P2-2   │ 符号执行集成 (angr)         │ 对特定关键函数做路径探索，精确但昂贵，按需使用               │
├────────┼─────────────────────────────┼──────────────────────────────────────────────────────────────┤
│ P2-3   │ ROP 约束求解编译器          │ 自动 exploit 生成的前置条件                                  │
└────────┴─────────────────────────────┴──────────────────────────────────────────────────────────────┘

不建议做的：
- 继续增加"浅层"解析模块（再加 10 个 parse_xxx 对研究能力提升不大）
- 自研符号执行引擎（集成 angr 即可）
- 自研反汇编器（Capstone 已经足够好）

---
总结：一句话

elf-tui 当前是一个 优秀的 ELF 数据浏览器，但还不是一个 漏洞研究推理引擎。差距在于：所有分析停留在语法层面，缺少从指令序列提升到语义层面（IR → 数据流 → 值域 → 约束）的关键跃迁。一旦完成 P0 的 IR 引入和 Trace 录制，现有 40+ 模块的价值会被重新激活——它们都将从"独立的信息展示器"变成"统一分析流水线的一个阶段"。